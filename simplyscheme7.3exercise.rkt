(define (sum-square a b)
  (let ((+ *)
        (* +))
    (* (+ a a) (+ b b))))

// gives you the sum of squares of two numbers
// first, assigns value of * to +, and * to + as temporary vars
// then, these vars are substituted into the body, and then primitive procedures are called on